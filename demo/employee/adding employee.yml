info:
  name: adding employee
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/v1/movies/?name="you"
  params:
    - name: name
      value: '"you"'
      type: query
  body:
    type: json
    data: |-
      {
        "rating":4.3,
        "name":"you"
      }  
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
